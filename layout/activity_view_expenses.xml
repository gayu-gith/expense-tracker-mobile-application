<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="#8A8CC6E0">

    <!-- Scrollable Area for Expense List -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fillViewport="true">

        <LinearLayout
            android:id="@+id/expense_list_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="8dp">
            <!-- Placeholder for Expenses -->
            <TextView
                android:id="@+id/placeholder_text"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:text="@string/no_expenses_found"
                android:textColor="#070707"
                android:textSize="16sp"
                android:textStyle="italic"
                android:visibility="gone" />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:paddingTop="8dp"
                android:text="@string/view_all_your_saved_expenses_here"
                android:textColor="#070707"
                android:textSize="30sp"
                android:textStyle="italic" />
        </LinearLayout>
    </ScrollView>

    <!-- Footer or Optional Actions -->
</LinearLayout>
